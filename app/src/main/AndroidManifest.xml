<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.wahyu.waitinglistapps">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity android:name=".View.Activity.DoctorListActivity"></activity>
        <activity android:name=".View.Activity.AddDoctorActivity" />
        <activity android:name=".View.Activity.ProfileActivity" />
        <activity android:name=".View.Activity.HelpActivity" />
        <activity android:name=".View.Activity.RegisterActivity" />
        <activity
            android:name=".View.Activity.HomeActivity"
            android:launchMode="singleTask" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity
            android:name=".View.Activity.LoginActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>